# 物理阶段索引

| target-id | 产物 | 说明 |
|-----------|------|------|
| [texture-atlas-editor-spa](./texture-atlas-editor-spa/) | 静态 Web（`dist/`） | Vue 3 + Vite SPA，纹理图集编辑器；VSIX 将同一份 `dist/` 同步至 `vscode-extension/dist/` 供 Webview 使用 |

源码根：`frontend-vue/`（相对仓库根）。
